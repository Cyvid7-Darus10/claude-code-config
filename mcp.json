{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"]
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp", "--browser", "chrome"]
    }
  },
  "_comments": {
    "context7": "Live documentation lookup for any library (used by docs-lookup agent). Zero config.",
    "playwright": "Browser automation and testing (used by e2e-runner agent). Zero config.",
    "github": "Removed — use the gh CLI instead; it is more context-efficient than a GitHub MCP server."
  }
}
